#include <iostream>
using namespace std;
int main() {
  int n, m, k;
  scanf("%d %d %d", &n, &m, &k);
  for (int i = 0; i < m; ++ i) {
    int s, e;
    scanf("%d %d", &s, &e);


  }

  for (int i = 0; i < k; ++ i) {
    int c;
    scanf("%d", &c);

  }
  return 0;
}
